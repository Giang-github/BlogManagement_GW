{% extends "base.html.twig" %}
{% block body %}
	<div class="right">
		<div class="right__content">
			<div class="right__title">Dashboard</div>
			<p class="right__desc">Insert Podcast</p>
			<div class="right__formWrapper">
				<form action="" method="post" enctype="multipart/form-data" id="formpost">
					<div class="containdiv">
						<div class="div">
                        {{ form_start(podcastForm) }}
							<div class="right__inputWrapper">
								<label for="title">Title</label>
								{{ form_widget(podcastForm.title,{
                                        'attr':{
                                        'placeholder': 'Title'
                                                }
                                 }) }}
							</div>
							<div class="right__inputWrapper">
								<label for="image">Image</label>
								{{ form_widget(podcastForm.image,{
                                 }) }}
							</div>
							<div class="right__inputWrapper">
								<label for="content">Content</label>
								{{ form_widget(podcastForm.content,{
                                        'attr':{
                                        'placeholder': 'Write something...'
										  }
                                 }) }}
							</div>
							<button class="btn" onclick="submit_blog()" type="submit">ADD PODCAST</button>
						</div>

						<div class="div">
							<div class="right__inputWrapper">
							<label for="category">Category</label>
								{{ form_widget(podcastForm.category,{
                                 }) }}
							</div>
							<div class="right__inputWrapper">
								<label for="course">Course</label>
								{{ form_widget(podcastForm.course,{
                                 }) }}
							</div>
							<div class="right__inputWrapper">
								<label for="audio">Audio</label>
								{{ form_widget(podcastForm.audio,{
                                 }) }}
							</div>
						{{ form_end(podcastForm) }}
					</div>
				</form>
			</div>
		</div>
	</div> 
{% endblock %}
