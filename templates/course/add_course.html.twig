{% extends "base.html.twig" %}
{% block body %}
	<div class="right">
		<div class="right__content">
			<div class="right__title">Dashboard</div>
			<p class="right__desc">Insert Course</p>
			<div class="right__formWrapper">
				<form action="" method="post" enctype="multipart/form-data">
					<div class="containdiv">
						<div class="div">
							{{ form_start(courseForm) }}
							<div class="right__inputWrapper">
								<label for="title">Title</label>
								{{ form_widget(courseForm.title,{
                                        'attr':{
                                        'placeholder': 'Title'
                                                }
                                 }) }}
							</div>
							<div class="right__inputWrapper">
								<label for="image">Image</label>
								{{ form_widget(courseForm.image,{
                                 }) }}
							</div>
							<div class="right__inputWrapper">
								<label for="description">Content</label>
								{{ form_widget(courseForm.description,{
                                        'attr':{
                                        'placeholder': 'Write something...'
										  }
                                 }) }}
							</div>

							<button class="btn" onclick="submit_blog()" type="submit">ADD COURSE</button>
						</div>
						<div class="div">
							{# <div class="right__inputWrapper">
								<label for="stardate">Start Date</label>
								{{ form_widget(courseForm.stardate,{
                                        'attr':{
                                        'placeholder': 'Start date'
                                                }
                                 }) }}
							</div> #}
							{# <div class="right__inputWrapper">
															<label for="enddate">End Date</label>
															{{ form_widget(courseForm.enddate,{
							                                 }) }}
														</div> #}
							{# <div class="right__inputWrapper">
								<label for="price">Price</label>
								{{ form_widget(courseForm.price,{
                                        'attr':{
                                        'placeholder': 'Enter price !!!.'
										  }
                                 }) }}
							</div> #}
						</div>
						{{ form_end(courseForm) }}
					</div>
				</form>
			</div>
		</div>
	</div>
{% endblock %}
